# Historical exported-code snapshot

This directory preserves the earlier flattened export for provenance and code
comparison. It is not the canonical execution surface and may contain older
copies of scripts that have since received path, CLI, or reproducibility fixes.

Run the scripts one directory above (`scripts/`) and the entry points documented
in the repository `README.md`. The standalone boost-sweep location is retained
as a compatibility import of the integrated canonical implementation.
